import CustomTextField from './CustomTextField';

// Export all custom components here
// These components can be used in jsonConfig.json5 with type: "custom"
export default {
    CustomTextField,
};
